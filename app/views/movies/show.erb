<%= link_to "Back to all reviews", movies_path %>
<h2><%= @movie.title %>
  (
<%= link_to "edit", edit_movie_path(@movie) %>

<%= link_to "delete", movies_path(@movie), method: :delete, confirm "really?" %>
  )
</h2>
<h2><%= @movie.title %></h2>
  <h4><%= @movie.director %></h4>
  <h4><%= @movie.runtime_in_minutes %></h4>
  <h4><%= @movie.description %></h4>
  <h4><%= @movie.poster_image_url %></h4>
  <h4><%= @movie.release_date %></h4>